services:
  # ─────────────────────────────────────────────
  # App Meteor (correos-webapp)
  # ─────────────────────────────────────────────
  meteor-app:
    build:
      context: .
      dockerfile: Dockerfile.meteor
    ports:
      - "9090:3000"
    environment:
      - MONGO_URL=mongodb://mongo:27017/correos
      - ROOT_URL=http://localhost:9090
      - PORT=3000
      - PRINTER_NAME=Canon_TS8300_USB
    depends_on:
      mongo:
        condition: service_healthy
      demonio-python:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - correos-net

  # ─────────────────────────────────────────────
  # Demonio Python (WebSocket + impresora)
  # ─────────────────────────────────────────────
  demonio-python:
    build:
      context: .
      dockerfile: Dockerfile.demonio
    ports:
      - "8000:8000"
      - "8001:8001"
    environment:
      # Backend de impresión: "auto", "cups" (Linux), "ipp" (Windows/red)
      - PRINTER_BACKEND=${PRINTER_BACKEND:-auto}
      # Para CUPS: nombre de impresora. Para IPP: IP o hostname de la impresora
      - PRINTER_1=${PRINTER_1:-Canon_TS8300}
      - PRINTER_2=${PRINTER_2:-Canon_TS8300}
      - PRINTER_TICKET=${PRINTER_TICKET:-Canon_TS8300}
    volumes:
      - ./demonio:/app
      - /var/run/cups:/var/run/cups:ro  # Solo necesario con backend CUPS en Linux
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.socket(); s.settimeout(2); s.connect(('localhost',8001)); s.close()"]
      interval: 15s
      timeout: 5s
      start_period: 10s
      retries: 3
    restart: unless-stopped
    networks:
      - correos-net

  # ─────────────────────────────────────────────
  # MongoDB
  # ─────────────────────────────────────────────
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 15s
      timeout: 5s
      start_period: 20s
      retries: 3
    restart: unless-stopped
    networks:
      - correos-net

volumes:
  mongo-data:

networks:
  correos-net:
